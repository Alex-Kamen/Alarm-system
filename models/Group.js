export class Group {
  constructor(treeName, groupIcon, objectList, type) {
    this.treeName = treeName;
    this.groupIcon = groupIcon;
    this.objectList = objectList;
    this.type = type;
  }
}
